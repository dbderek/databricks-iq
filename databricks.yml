bundle:
  name: databricks-iq

run_as:
  - user_name: derek.witt@databricks.com

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://fe-vm-buzzforce-demo-workspace.cloud.databricks.com/
      root_path: /Workspace/Users/derek.witt@databricks.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      dbsql_serverless_warehouse_id: aaba42c163156b19

  prod:
    mode: production
    default: true
    workspace:
      host: https://fe-vm-buzzforce-demo-workspace.cloud.databricks.com/
      root_path: /Workspace/Users/derek.witt@databricks.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      dbsql_serverless_warehouse_id: aaba42c163156b19